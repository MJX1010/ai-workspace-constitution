# Claude 工作区规则

本文件是 `<工作区根>` 顶层规则的 Claude 版本。若更近目录存在 `AGENTS.md`、`CLAUDE.md` 或本地 skills，优先使用更近规则。

## 工作区模型

`<工作区根>` 是多仓库工作区。不要假设所有目录共享同一套架构、工具链或依赖模型。

用户在当前对话 session 中声明了具体工程路径时，先规范化为绝对路径，并把它作为本轮目标根目录；环境探测、VCS、构建、代码生成、review 和文档同步命令默认从该根目录或其明确子目录执行。不要猜测或拼接其它工程根，例如把 `<用户声明的工程根>` 误当成 `<工作区根>/Trunk`。只有维护顶层治理、共享 specs 或共享 skills 时，才回到 `<工作区根>`。

新 session 开始或用户重新指定工程路径时，必须判断该路径是否为工程根目录：优先看 `.git` / `.svn`，其次看 Unity `Assets` + `ProjectSettings`、`.sln` / `.csproj`、`package.json`、`pyproject.toml`、`Cargo.toml` 等根标志。若确认是工程根且 codegraph 可用，未初始化则先 `codegraph init`，已有索引则刷新/更新索引，并确认新改动文件已进入索引；若只能访问 codegraph 查询工具，则用 `codegraph_status` / `codegraph_explore` 确认索引状态。codegraph 不可用、索引过期或不支持当前语言时，必须说明降级，并用 `rg`、语言服务、编译/测试和项目本地 review gate 补足影响面检查。

改代码前确认：

1. 目标仓库
2. 最近规则文件
3. 归属模块
4. source of truth
5. 验证路径

从 `<工作区根>` 顶层对某个子目录项目发起 AI agent 对话协作时，必须按路径自上而下逐层发现上下文：先看顶层 `AGENTS.md` / `CLAUDE.md`、`.codex/skills/`、`.claude/skills/`、`.agents/skills/`、`.skills/` 以及共享 `docs` / `references`；再进入目标子目录，逐层查找更近的 `AGENTS.md` / `CLAUDE.md`、本地 skills、项目 docs、references、README、specs 等。越靠近目标目录的规则优先；顶层内容只作为默认上下文，不覆盖子项目的 source of truth。

## 交互与方案选择

- 与用户沟通默认使用简体中文；必要技术名词、路径、命令、API 名称保留原文。
- 目标明确时，选择长期总成本最低的方案，而不是当前看起来最快的方案；但问题规模小、复用概率低、风险边界清晰时，直接使用最简单方案。
- 好的方案应简单、实用、稳定、可验证、可维护：概念少，只解决当前目标，行为可预测，失败模式清楚，能用测试、构建、日志或数据证明正确，修改点集中且没有隐藏耦合。
- 从第一性原理出发，先判断真实目标、关键假设、source of truth 和最小可验证闭环；警惕把手段误当目标的 XY 问题。
- 当错误前提、目标不清或低效路径会影响目标或决策时，必须指出具体问题、影响、缺失信息和替代方案；不能判断时明确说明缺什么。
- 执行边界：目标清晰且路径合理则直接执行；路径明显不优则先给更优路径，并在不影响任务时继续完成；目标不清、验收缺失、风险过高或影响不可逆时先提问。
- 回答默认结论优先、理由随后，不做空泛铺垫；用户明确要求特定格式时，以用户当前格式为准。
- 在 Paseo 中引用 Windows 本地文件时，Markdown target 只能包含使用正斜杠的绝对文件路径，不得追加 `:行号`、`:列号` 或 `#L行号`；Paseo 会将行号后缀当作文件名并导致 `ENOENT`。需要标注位置时，把“第 N 行”写在链接文本或链接后的普通文本中。
- 涉及实际代码或工程改动时，说明改了什么、验证了什么、剩余风险是什么。仅在需要挑战假设、指出长期风险、给出低成本替代方案或补充关键决策时，追加“深度交互”小节；否则省略，不写占位文字。

## 核心约束

### 最近规则覆盖顶层默认

顶层规则只提供通用约束；真实架构决策以仓库内规则为准。

### 不跨仓库套用约定

Unity、ET、自动化、工具仓库可能相似，但 ownership、codegen、验证方式可能不同。

### 不直接改生成物

生成文件、导出文件、缓存输出、codegen 结果，在证明它就是 source of truth 前都视为受保护产物。

### 配置 source of truth

手写配置文件必须有且只有一个可手改权威源。Editor/Runtime、脚本/构建、客户端/工具需要共用时，优先读取同一份配置；确需派生快照或代码时，派生产物必须自动生成、不可手改、可校验同步，并在文档中标明权威配置路径。

### 故障分析先证据闭环

报错日志只能作为排查入口，不能直接等同根因。涉及 Jenkins、SVN externals、Unity 构建、SDK 导入、版本源、CDN/FTP/SFTP 发布、运行时资源加载等跨系统问题时，改代码前必须先建立证据链：对比最近成功/失败构建、构建快照与当前项目配置、Jenkins 工作副本实际状态、SVN 服务端与本地状态、相关 source of truth、客户端运行日志和可复现路径。证据不足时先继续只读检查或提出假设清单，不要根据单条错误日志马上修改脚本、参数、模板或工作流。

紧急绕过只能是显式标注的临时操作，必须说明未验证风险和回退方式；不能把临时绕过沉淀成默认规则或提交到仓库。

### Jenkins/SVN 构建清理与上传治理

Jenkins 管理 SVN 工作副本时，不默认 `deleteDir()` 清整个 workspace。优先只对 SCM 管理的 checkout 子目录执行 `svn cleanup`、`svn revert`、`svn update` 或 Jenkins SVN `UpdateUpdater` 原地更新；`Logs/`、归档、杀软或外部进程可能持有句柄，不能让这些临时目录导致下一轮 checkout 失败。

构建产物必须先落到本地受控归档目录，例如 `ARTIFACTS_ROOT/<build_number>` 或项目约定的 artifacts 目录；`Logs/` 可以保留日志，但不要把 APK/AAB/IPA/ZIP 等正式产物只放在日志目录。FTP/SFTP/CDN 上传失败默认视为发布失败或不稳定状态，不等同于 Unity 编译失败；CI 应保留本地产物并提供后续手动补传入口。

上传凭据只能维护在后端环境、Jenkins Credentials 或项目认可的密钥系统中，不作为构建参数、前端表单、日志或 Markdown 明文传播。手动补传、重新发布、发布构建等外部副作用操作执行前仍需用户明确授权。

### Unity 文本资产写入

直接写入 Unity 文本资产时，包括 ScriptableObject `.asset`、Prefab、Scene、ProjectSettings 以及 `*_UIFeatureDoc.asset` 等 YAML 文件，必须保持 Unity 序列化结构、GUID/fileID、缩进和字段结构稳定；新增或变更的中文及其它非 ASCII 字符保存为 `\uXXXX` 转义形式。导出的 Markdown、docs、skills 保持可读中文，不把 Markdown 的中文原文反向写进 Unity asset。

能通过 Unity API、`SerializedObject`、`PrefabUtility`、`AssetDatabase` 修改的资产，优先走 Unity 序列化接口并 `SetDirty` / `SaveAssets`；只有明确知道字段结构且改动很小时才直接 patch YAML，完成后扫描目标 asset 中是否残留非 ASCII 字符。

### 避免失控扫改

没有明确影响范围和验证策略，不做大规模跨仓库编辑。

禁止保留“顺手改动”：不属于用户当前目标的格式化、重生成、字段重排、默认值调整、profile 切换、路径改写、shell/interpreter 改写、构建 stage 改写，都先视为无关风险。保留前必须确认它是当前目标必需、来自正确 source of truth、并有明确验证路径；否则收窄为最小补丁或先询问用户。

生成器、模板和批量同步工具的输出必须先看 diff 再采纳。若生成结果夹带环境 profile、执行脚本、凭据、路径、平台、产物格式或部署目标变化，不能因为工具生成了就保留，只能提取当前需求需要的最小差异。

### 治理文档必须中文，减少上下文

编写或更新 `AGENTS.md`、`CLAUDE.md`、skills、docs 时，**必须使用简体中文**；必要技术名词保留英文。英文治理文档视为不合格，需翻译后才能使用。

这些文件要短、稳定、低上下文占用：写规则，不写长背景；避免重复；能引用就不要整段复制；示例保持短且可执行。

仅当用户明确要求产出文档、报告、方案、说明书、复盘、规范等文档类交付物，或任务本身明确以文档为产物时，才生成文档文件。此类交付默认使用双格式：先生成可维护的 `.md` 源文档，同时生成一份可直接浏览的自包含 `.html`，参考 <https://thariqs.github.io/html-effectiveness/> 的做法。两份文档正文默认使用简体中文；Markdown 负责版本管理和后续编辑，HTML 负责更高信息密度的阅读、演示、图表、时间线、对比和轻交互。普通代码修改、问答、review、排障不自动额外生成 `.md` 或 `.html`。

### 沉淀可复用知识

发现稳定工作流或规则时，更新最近合适的治理文件或 skill。

共享 skill 命名必须按能力、框架或工作流命名，不写项目名前缀；项目限定条件写入触发场景或更近的项目本地 skill。skill 正文、示例和元数据不得写入本机绝对工程路径、个人用户名目录或 Windows 盘符路径；使用会话声明的目标根、`<项目根目录>` / `<Unity工程根>` 等占位变量或相对路径。

Skill 目录分工：

- 新增 skill 一律创建在 `.skills/`
- 修改通用 skill 时优先修改 `.skills/`
- `.codex/skills/`、`.claude/skills/` 只作为对应工具加载或同步后的消费目录；除非用户明确要求修复镜像同步问题，不在其中新建 skill
- `.agents/skills/` 视为历史或第三方只读目录；本工作区任务不得在其中创建、修改或同步 skill

### 飞书文档读取与本地归档

- 遇到飞书、Lark、豆包文档链接，优先使用目标项目最近的 `feishu-doc-reader` 或同类 skill，并按 `lark-cli` 内置 skill 文档确认命令；不要把登录页、摘要、旧缓存或截图局部当成正文。
- 用户要求完整读取时，正文、结构、图片/附件/表格线索未确认前不得进入实现；无法读取时先说明授权、配置或权限缺口。
- 抓取内容必须归档在目标项目内的受控临时证据目录，例如 `<Unity工程根>/Temp/UI_AutoGen/<module>_lark/` 或 `<项目根目录>/Temp/Feishu/<feature>/`，包含来源元数据、原始 Markdown/XML、媒体/表格清单和未读项说明。
- 临时归档不存 App Secret、user token、refresh token，不默认提交；稳定结论再同步到 UIDoc、docs 或 specs。用户说明文档更新时必须重新抓取或写明使用的快照版本。

### 完成前本地 review

创建或修改代码后，完成回复前运行最近的仓库本地 review gate，并把结果带回当前对话。

`<工作区根>/DragonPow2` 下如存在 `Tools/codereview/cr_local.ps1`，从项目根目录运行：

```powershell
.\Tools\codereview\cr_local.ps1 -Path @('path\to\changed-file.cs')
```

review 范围包含改动文件及相关 caller、callee、references、生成项目元数据、skill/tool 镜像。不要发送飞书通知。RED 发现必须修复后再完成。

### 环境与编码基线

跨仓库通用环境/编码纪律；工具实现见 `ai_projects/ai_cli_profiles/profiles/ai-helpers.ps1`（PowerShell `$PROFILE` 链式加载）。

- 开工先 `ai-env-probe`（别名 `aienv`）：以实测 cwd/HOME 为准，不信任注入的平台/路径元数据；判定 SVN 还是 Git 再选命令。
- 临时文件按用途选落点：项目产物、生成中间件、工具要求相对路径时，放在目标工程内的受控临时目录（例如 Unity 项目的 `Temp/UI_AutoGen/`），不要放到无关仓库或 `<工作区根>` 顶层；一次性 helper 脚本可放 `$env:TEMP`，但仅限工具明确接受绝对路径且脚本不会成为项目 source of truth 的场景。
- Windows 中文统一 UTF-8：`chcp 65001` + 控制台/`$OutputEncoding` UTF-8、`PYTHONUTF8=1`；PowerShell 启动 helper 必须为 `Get-Content` / `Set-Content` / `Add-Content` / `Out-File` 等文本命令设置 UTF-8 默认编码，避免 PS 5.1 回落到 ANSI 或 UTF-16LE。
- **含中文的 `.ps1` 存 UTF-8 with BOM**（PS 5.1 否则乱码）；给程序读的 JSON/TOML 写无 BOM；需要无 BOM 写文本时优先用 `Write-Utf8NoBom`，不要依赖 PS 5.1 的 `Set-Content` / `Out-File` 默认行为。
- 中文是硬性质量门禁：SVN/Git 提交日志、构建说明、Jenkins/TortoiseSVN 展示文本、部署说明和变更摘要中禁止出现乱码、Unicode replacement character（U+FFFD）、`??` 占位或无法确认编码的中文；一旦发现必须先停下来定位编码边界，不能继续提交、发布或把乱码写入历史。
- Windows/PowerShell 下提交中文 SVN 日志时，必须把日志写入 UTF-8 文本文件并使用 `svn commit -F <日志文件> --encoding utf-8`；自动提交脚本禁止拼接 `svn commit -m "中文..."`。提交后用 `svn log -r <rev> --xml` 或 TortoiseSVN 验证中文可读且无 U+FFFD/问号占位。
- **禁止提交 `AGENTS.md` / `CLAUDE.md`**：这些治理入口文件默认只能本地修改用于当前协作上下文，不能纳入 SVN/Git commit、import、copy、push 或自动提交。只有用户在当前对话中明确点名“提交 AGENTS.md/CLAUDE.md”并给出单独授权时，才允许提交指定文件；普通“提交”“继续”“同步规则”都不构成授权。
- 任何 `svn commit`、`git commit`、`svn import`、`svn copy`、push、上传制品、发布构建、触发线上/后台提交类操作，执行前必须单独询问用户并得到明确确认；不能把“继续”“修复”“帮我处理”理解为提交或上传授权。
- 涉及 Unity 客户端、多平台构建、补丁、CDN、Jenkins 或发布入口的改动，提交前必须明确受影响平台矩阵并完成本地可行的编译/测试；提交后必须触发并确认 Android/iOS 等受影响平台构建通过，失败则继续修复，不能把未验证或未通过状态写成已完成。
- 会话续接用 `ai-handoff -Project -Module -Purpose`（项目_模块_用途_日期）；所有交接产物统一保存到 `<工作区根>/ai_handoff/<项目_模块_用途_日期>/`。该路径覆盖通用 `handoff` skill 的系统临时目录默认值；除非用户当前明确指定其它位置，不得写入 `$env:TEMP`、项目仓库或旧的 `<工作区根>/_handoff/`。

## 推荐风格

共享治理文档、skills、项目 docs 默认用简体中文；技术名词可保留英文以保证准确。

## 相关文件

- `AGENTS.md`
- `GEMINI.md`
- `.claude/skills/`
- `.agents/skills/`
- `.skills/`
- `.codex/skills/workspace-governance/`
- `.codex/skills/skill-factory-playbook/`
